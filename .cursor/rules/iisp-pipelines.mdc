---
description: IISP Pipeline / Kestra Flow 编排规范 — 仅 registered tool_id、Kestra 生产路径
globs: iisp-catalog/pipelines/**/*,catalog_cache/pipelines/**/*
alwaysApply: false
---

# IISP Pipeline / Kestra Flow

规范：`docs/IISP_DESIGN_FINAL.md` v2.2 · `docs/TOOLBOX_ORCHESTRATION.md` · `docs/CODING_STANDARDS.md` §5

## 必须

- **生产编排**：`iisp-catalog/pipelines/kestra/`（Kestra Git sync）
- 每个步骤调用的 `tool_id` 存在于 Registry
- Kestra：每步 `POST /v1/tools/{id}/invoke`；人工卡点用 **Pause**
- 设计态 DSL：`pipelines/demo/`、`legacy/` 需编译或同步到 `kestra/`

## 禁止

cron 生产调度、Windmill、YAML 内嵌 Python/SQL、编造 tool_id

## 提交前

```bash
./scripts/iisp workflow validate path/to/flow.yaml
```

Skill：**iisp-compose-flow**
