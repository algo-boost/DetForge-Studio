---
alwaysApply: true
---
# IISP 项目级约束

实现与文档以 **`docs/IISP_DESIGN_FINAL.md`** 为唯一架构定稿。

## 三条铁律

1. **编排零自研 DAG** — **唯一编排器：Kestra**（Edge + Hub）
2. **契约唯一** — 集成只通过 `POST /v1/tools/{id}/invoke` 与 Catalog Pipeline YAML
3. **AI 只写文件** — 仅改允许路径；运行态无 LLM  
4. **变更必记录** — 用户可见功能与契约/部署变更须同 PR 更新 [`docs/PLATFORM_CHANGELOG.md`](docs/PLATFORM_CHANGELOG.md)（Skill：**iisp-record-platform-change**）

## 禁止（PR 应拒绝）

- 扩展 `workflow_engine` / `workflow_scheduler` 作主编排
- Tool 之间 `import` 对方 `service.py`
- 在 Gateway 写业务组合 if-else
- 跳过 `iisp tool validate` / `iisp workflow validate`
- 提交 `config.json`、密钥
- **功能已改但未更新 `PLATFORM_CHANGELOG.md`（且无「无用户可见变更」说明）**

## 必读文档

| 文档 | 用途 |
|------|------|
| `docs/CODING_STANDARDS.md` | 编码规范与技术选型 |
| `docs/SECURITY.md` | Token 与安全 |
| `docs/ARCHITECTURE_DIAGRAMS.md` | 架构图 |
| `AGENTS.md` | Agent 贡献入口 |

## 项目 Skills（`.cursor/skills/`）

| Skill | 场景 |
|-------|------|
| `iisp-vibe-guardrails` | 任何 IISP 开发前自检 |
| `iisp-create-tool` | 新 Tool |
| `iisp-compose-flow` | 新 Pipeline |
| `iisp-review-pr` | PR 前审查 |
| `iisp-record-platform-change` | **平台功能变更同步记录** |
| `iisp-platform-core` | 仅平台组改 Core |

## 代码原则

最小 diff、匹配现有风格、不 over-engineer（与 **karpathy-guidelines** 一致）。业务扩展 = Tool PR + Pipeline PR，不改 Platform 调度内核。

## 通用编码行为（Karpathy）

项目已安装 [forrestchang/andrej-karpathy-skills](https://github.com/forrestchang/andrej-karpathy-skills)：

- Rule：[`.cursor/rules/karpathy-guidelines.mdc`](karpathy-guidelines.mdc)（`alwaysApply: true`）
- Skill：[`.cursor/skills/karpathy-guidelines/`](../skills/karpathy-guidelines/SKILL.md)

冲突时：**IISP 架构/domain** 优先；**简洁/ surgical diff** 遵循 Karpathy。
