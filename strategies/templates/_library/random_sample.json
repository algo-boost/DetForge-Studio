{
  "id": "random_sample",
  "name": "随机采样",
  "description": "从结果中随机抽取指定数量的样本",
  "icon": "shuffle",
  "params_schema": [
    { "key": "max_rows", "env_key": "SAMPLE_SIZE", "label": "最大条数", "type": "number", "default": "", "min": 1, "max": 10000, "step": 1, "description": "留空表示不采样；由环境变量 SAMPLE_SIZE 控制" },
    { "key": "random_seed", "env_key": "RANDOM_SEED", "label": "随机种子", "type": "number", "default": "42", "min": 0, "max": 9999, "step": 1 }
  ],
  "python_code": "def filter(df, params):\n    raw = get_env('SAMPLE_SIZE', '').strip()\n    if not raw:\n        return df.reset_index(drop=True)\n    max_rows = int(raw)\n    seed = int(get_env('RANDOM_SEED', '42') or 42)\n    if max_rows <= 0 or len(df) <= max_rows:\n        return df.reset_index(drop=True)\n    return df.sample(n=max_rows, random_state=seed).reset_index(drop=True)"
}
